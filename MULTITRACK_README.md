# 连线录屏的主播 / 嘉宾区分

软件现在支持 OBS 多音轨录屏：每条音轨会单独提取、转写，再合并成带标签的结果。

默认标签为：

```text
音轨 1 -> 主播
音轨 2 -> 连线嘉宾
```

## OBS 建议设置

1. 打开 `设置 -> 输出 -> 录制`，启用至少两条音频轨道。
2. 打开 `编辑 -> 高级音频属性`。
3. 主播麦克风只勾选音轨 1。
4. 浏览器/连线声音只勾选音轨 2。
5. 背景音乐建议单独放到音轨 3，避免混入说话内容。

如果视频只有一条混合音轨，软件会输出 `[混合音轨]`，不会自动猜测说话人。此时仍需要真正的说话人分离模型或人工校对。

结果中的 `transcript.json` 会包含 `speaker` 字段，`transcript.txt` 和 `transcript.srt` 会显示：

```text
[主播] 这是主播说的话
[连线嘉宾] 这是连线嘉宾说的话
```
